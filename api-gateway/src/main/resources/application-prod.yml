server:
  port: 80

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/gatewaydb
    username: gateway
    password: gateway1234

  data:
    redis:
      host: localhost
      port: 6379

gateway:
  name: DH-APIGW
  environment: PROD

rate-limit:
  enabled: false
  requests-per-minute: 60
